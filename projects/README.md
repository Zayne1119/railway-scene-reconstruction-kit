# Local production projects

Create projects here with `railway-recon init projects/<project-id>`. Every child directory is ignored by Git because it may contain raw point clouds, panoramas, coordinates and generated models.

Do not remove the ignore rule to make data transfer easier. Use the organization's approved secure storage for production inputs and releases.

