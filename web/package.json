{
  "name": "railway-recon-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {"node": ">=22"},
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3010",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3010"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}

