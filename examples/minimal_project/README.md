# Minimal synthetic project contract

This example contains no real point cloud or imagery. It demonstrates the expected camera CSV and asset registry formats. Use `railway-recon init projects/my-project --name "My Project"` for a writable project and place production inputs only in the ignored project directory.

