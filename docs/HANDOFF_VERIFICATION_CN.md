# 新人交接验证记录

日期：2026-09-05。本记录只覆盖可迁移的工具包安装、合成演示和本机网页，不证明现场重建精度，也不使用客户输入。

当前实现已完成：统一入口、依赖锁安装、只读体检、合成 LAZ 到 GLB、独立网页配置、源码白名单交接、中文操作说明。

验证范围与限制：

- Windows PowerShell 5.1 的 21 项集成测试通过，覆盖含空格目录、引号/空参数、失败退出码、未知目录保护、ZIP 校验与真实超过 260 字符的路径；未在 macOS/Linux 验证一键脚本。
- 演示实际生成 2,726 个点、80 个具名对象和 6,096 个三角面；重复运行校验通过。
- 网页实际 HTTP 测试检查本机监听、必需/可选资源、403 安全边界及端口冲突失败；不把 HTTP 200 当作可见质量验收。
- 本轮浏览器插件没有可连接的浏览器，无法完成页面视觉与点击验收；该项明确未验证，不冒充已看到模型。
- 初始网络权限/超时失败没有被报告为成功。获得本次网络权限后，官方 uv 0.12.1 和 Node 22.23.2 ZIP 下载、SHA-256 校验、解压均实际通过；uv 自动下载并安装项目内 Python 3.11.15 也通过。Node 深目录解压曾暴露 Windows 旧路径长度限制，已通过 extended-length 目标路径修复，不修改系统策略。
- 两次源码副本均不携带旧 `.venv`、`node_modules`、`.runtime` 或实际网页项目配置。第一份用明确指定的现有运行时安装；第二份位于含空格路径，自动安装新的项目内 Python，再从官方源安装 19 个 Python 包与 15 个 npm 包。
- 第二份副本全链验证：`setup` → `demo -SkipSetup -NoStart` → `check`。**762 项 Python 测试通过，2 项跳过；18 项网页测试通过，网页构建成功。** 跳过原因是 Windows 无创建符号链接权限、未安装可选 Open3D 研究基线；不需要为基础演示补装它们。长路径提取最后新增的 2 项回归另行验证，包含在上面的 21 项启动器测试中。
- 第一次副本回归发现遗漏 6 份合成质量门禁 fixture，已补进明确白名单，再用第二份全新副本完整验证；没有删除失败记录或跳过失败测试。原有 Pillow 弃用提示与 Three.js 包体积提示仍保留，不冒充零告警。
- 两份新目录生成的 GLB 均与开发目录一致：`87ac19a6606336bc9966d98166b5862addd5f436422c9c75cb59fc1794af5377`。真实 Three.js GLTFLoader 在 CPU 上验证 80/80 资产绑定、包围盒与射线拾取，图层为轨道 70 + 站场 10；并修复竖屏自动取景略裁边的问题。这不是 WebGL 视觉验收。
- 上述是**同一台 Windows 电脑的新目录/新环境验证与分支实测**，不是另一台物理电脑、任意企业网络或完全无工具机器的一次性联合验证。

不要求用户完成逐图人工签核；上述缺口是验证边界，不是新增的日常审核工单。
