# Schemas

The authoritative runtime schemas are bundled under `src/railway_recon/resources/` so they are available after package installation. The same files can be exported by a release process when another system needs standalone JSON Schema documents.

